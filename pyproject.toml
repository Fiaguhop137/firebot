[project]
name = "firebot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pludunn", email = "291398076+pludunn@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pip>=26.2",
    "pygame>=2.6.1",
]

[project.scripts]
firebot = "firebot:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
